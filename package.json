{
  "name": "chainvolio-web-workspace",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "npm run dev --workspace=@chainvolio/web",
    "build": "npm run build --workspace=@chainvolio/web && cp -r apps/web/.next .next"
  }
}
